package de.dewarim.goblin

/**
 * Result object for sending email. 
 * May encapsulate detailed messages in the future, so it's better to use this now 
 * instead of simple boolean signalling.
 */
class SendMailResult {
    
    boolean okay = false
    
}
